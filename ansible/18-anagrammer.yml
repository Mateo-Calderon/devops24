---
- name: Run custom anagrammer module
  hosts: localhost
  gather_facts: false

  vars:
    message: "hello world"

  tasks:
    - name: Run the anagrammer module
      anagrammer:
        message: "{{ message }}"
      register: result

    - name: Show result
      ansible.builtin.debug:
        var: result
